import PagePlaceholder from "@/components/PagePlaceholder";

export default function ResultsPage() {
  return (
    <PagePlaceholder
      title="Results"
      milestone="M5"
      note="Testimonials hub, sourced from Sanity once the CMS is wired in M4."
    />
  );
}
