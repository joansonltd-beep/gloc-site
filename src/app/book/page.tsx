import PagePlaceholder from "@/components/PagePlaceholder";

export default function BookPage() {
  return (
    <PagePlaceholder
      title="Book"
      milestone="M7"
      note="Booking module slot. Wired up later once lead volume justifies it (spec.md §12)."
    />
  );
}
