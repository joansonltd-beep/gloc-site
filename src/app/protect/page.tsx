import PagePlaceholder from "@/components/PagePlaceholder";

export default function ProtectPage() {
  return (
    <PagePlaceholder
      title="Protect"
      milestone="M2"
      note="Life · Health · Critical Illness, plus the Protection Planner tool."
    />
  );
}
